{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsAngularGuard",
  "title": "Angular Guard Options Schema",
  "type": "object",
  "description": "Creates a new Angular guard with .guard suffix",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the guard.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the guard?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the guard."
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "When true, creates files at the top level of the project."
    },
    "skipTests": {
      "type": "boolean",
      "default": false,
      "description": "Do not create \"spec.ts\" test files for the new guard."
    },
    "functional": {
      "type": "boolean",
      "default": true,
      "description": "Creates a functional guard."
    }
  },
  "required": ["name"]
}
