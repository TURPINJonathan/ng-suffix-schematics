{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsAngularService",
  "title": "Angular Service Options Schema",
  "type": "object",
  "description": "Creates a new Angular service with .service suffix",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the service.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the service?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service."
    },
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "flat": {
      "type": "boolean",
      "default": false,
      "description": "When true, creates files at the top level of the project."
    },
    "skipTests": {
      "type": "boolean",
      "default": false,
      "description": "Do not create \"spec.ts\" test files for the new service."
    },
    "type": {
      "type": "string",
      "description": "Adds a developer-defined type to the filename, in the format \"name.type.ts\".",
      "default": "Service"
    }
  },
  "required": ["name"]
}
